<Window x:Name="PageSwitcher" x:Class="PatientRecordsWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Patient Records" Height="600" Width="1200" Activated="PageSwitcher_Activated">
    
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FFCCCCCC" Offset="0"/>
                <GradientStop Color="White" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="15*" />
            </Grid.ColumnDefinitions>

            <Button Style="{StaticResource LinkButton}" x:Name="btnAdd" Click="btnAdd_Click" Content="new" >
            </Button>
            <Button Style="{StaticResource LinkButton}" x:Name="btnSearch" Grid.Column="1" Click="btnSearch_Click" Content="search">
            </Button>  
        </Grid>
        <Frame x:Name="frameContainer" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
