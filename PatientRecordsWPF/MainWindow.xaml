<Window x:Name="PageSwitcher" x:Class="PatientRecordsWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Patient Records" Height="350" Width="525" Activated="PageSwitcher_Activated">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button  x:Name="btnSearch" Click="btnSearch_Click">
                <Button.Background>
                    <ImageBrush ImageSource="Resources/Search.png"/>
                </Button.Background>
            </Button>            
            <Button x:Name="btnAdd" Grid.Row="1" Click="btnAdd_Click" >
                <Button.Background>
                    <ImageBrush ImageSource="Resources/Add.png"/>
                </Button.Background>
            </Button>

        </Grid>
        <Frame x:Name="frameContainer" Grid.Column="1" />
    </Grid>
</Window>
