<Window x:Name="PageSwitcher" x:Class="PatientRecordsWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Patient Records" Height="600" Width="800" Activated="PageSwitcher_Activated">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button  x:Name="btnSearch" Click="btnSearch_Click" Content="Search">
            </Button>            
            <Button x:Name="btnAdd" Grid.Row="1" Click="btnAdd_Click" Content="New" >
            </Button>

        </Grid>
        <Frame x:Name="frameContainer" Grid.Column="1" />
    </Grid>
</Window>
